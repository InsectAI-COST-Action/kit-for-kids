# Keep repository text files consistent across Windows and macOS.
* text=auto eol=lf

# Treat firmware/dashboard/data assets as binary when present.
*.bin binary
*.elf binary
*.jpg binary
*.jpeg binary
*.png binary
*.gif binary
*.tflite binary

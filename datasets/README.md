# Local computer-vision datasets

Dataset layouts, split manifests, annotation guidance, and labels are tracked here. Source images are intentionally ignored by Git because camera datasets can be large and may require separate review before any public sharing. Use `tools/build_ant_detector_dataset.py` to recreate an image split from a mounted card.
